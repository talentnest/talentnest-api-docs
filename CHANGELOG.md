# TalentNest API changelog

Changes that affect integrators. Generator (Slate) version bumps do not belong here.

## 2026-09-11

* Corrected HTTP request URLs that used `subodmain` instead of `subdomain`.
